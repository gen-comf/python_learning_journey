#!/usr/bin/env python3
"""
Проект 2: Магический шар 8

Программа имитирует магический шар (шар судьбы) — игрушку для предсказания будущего.
Пользователь задаёт вопрос, программа случайным образом отвечает.
"""

import random

# Константа с ответами (принято именовать заглавными буквами)
ANSWERS = [
    "Бесспорно",
    "Предрешено",
    "Никаких сомнений",
    "Определённо да",
    "Можешь быть уверен в этом",
    "Мне кажется — «да»",
    "Вероятнее всего",
    "Хорошие перспективы",
    "Знаки говорят — «да»",
    "Да",
    "Пока не ясно, попробуй снова",
    "Спроси позже",
    "Лучше не рассказывать",
    "Сейчас нельзя предсказать",
    "Сконцентрируйся и спроси опять",
    "Даже не думай",
    "Мой ответ — «нет»",
    "По моим данным — «нет»",
    "Перспективы не очень хорошие",
    "Весьма сомнительно"
]


def get_random_answer():
    """Возвращает случайный ответ из списка ANSWERS."""
    return random.choice(ANSWERS)


def ask_question(user_name):
    """
    Запрашивает вопрос у пользователя и выводит случайный ответ.
    
    Args:
        user_name (str): Имя пользователя для персонализации
    """
    print(f'\n{user_name}, какой у тебя вопрос?')
    question = input().strip()
    
    if question:  # Проверка на пустой вопрос
        print(f'🎱 Ответ: {get_random_answer()}')
    else:
        print("Кажется, ты ничего не спросил...")


def should_continue(user_name):
    """
    Спрашивает пользователя, хочет ли он продолжить.
    
    Args:
        user_name (str): Имя пользователя
    
    Returns:
        bool: True если продолжать, False если нет
    """
    while True:
        response = input(f"\n{user_name}, хочешь задать еще вопрос? (да/нет): ")
        response = response.strip().lower()
        
        if response == 'да':
            return True
        elif response == 'нет':
            return False
        else:
            print("Пожалуйста, ответьте 'да' или 'нет'")


def main():
    """Основная функция программы."""
    print("=" * 50)
    print("Привет Мир, я магический шар, и я знаю ответ на любой твой вопрос.")
    print("=" * 50)
    
    # Получаем имя пользователя
    user_name = input("Напиши свое имя: ").strip()
    if not user_name:  # Если имя пустое
        user_name = "Друг"
    
    print(f'\nПривет, {user_name}!')
    print("Задай мне любой вопрос, и я открою тебе тайны вселенной!")
    
    # Основной игровой цикл
    while True:
        ask_question(user_name)
        
        if not should_continue(user_name):
            print(f'\n{user_name}, возвращайся, если возникнут вопросы!')
            print("До свидания! 👋")
            break


if __name__ == "__main__":
    main()
    