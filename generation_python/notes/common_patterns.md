# Часто встречающиеся паттерны в задачах

## Для работы с цифрами числа
### 1. Извлечение цифр из 3-значного числа:

n = 123
a = n // 100      # 1
b = n // 10 % 10  # 2
c = n % 10        # 3

### 2. Извлечение цифр из n-значного числа:
# результатом операции num % 10 является последняя цифра числа
# результатом операции num // 10 является число с удалённой последней цифрой
(num // 10 ** (n - i)) % 10     
# где n – количество разрядов числа, i – номер цифры числа (слева направо, начиная с 1).

num = int(input())                                
# считываем число
while num != 0:                                   
# проверяем, что цифры числа не закончились
    last_digit = num % 10                         
# получаем последнюю цифру числа
    ...                                           
# обрабатываем последнюю цифру числа
    num = num // 10                               
# удаляем последнюю цифру из числа

## Из задач раздела "Типы данных":
### 1. Обратное число:
n = 123
reverse = int(str(n)[::-1])  # 321
### 2. Дробная часть числа:
n = 3.1415
fractional = n - int(n)  # 0.1415
### 3. Расстояние Манхэттена:
x1, y1, x2, y2 = map(int, input().split())
distance = abs(x1 - x2) + abs(y1 - y2)
### 4. Сортировка трех чисел:
a, b, c = map(int, input().split())
sorted_nums = sorted([a, b, c])
print(*sorted_nums)
### 5. Арифметические формулы:
(a + b) / 2                         # среднее арифметическое чисел
sqrt(a * b)                         # среднее геометрическое чисел
(2 * a * b) / (a + b)               # среднее гармоническое чисел
sqrt((a ** 2 + b ** 2) / 2)         # среднее квадратичное чисел
