# 📚 Шпаргалка по строкам Python
*На основе раздела "Строки" курса "Поколение Python"*

## 🔤 Создание и базовые операции
```python
s1 = 'Python'       # Одинарные кавычки
s2 = "строка"       # Двойные кавычки
s3 = input()        # Ввод с клавиатуры
empty = ''          # Пустая строка


🎯 Индексация и срезы
Паттерн	Пример	Результат	Описание
s[i]	"Python"[2]	't'	Символ по индексу
s[-i]	"Python"[-1]	'n'	С конца
s[start:end]	"Python"[2:5]	'tho'	Срез (end не включён)
s[start:]	"Python"[2:]	'thon'	До конца
s[:end]	"Python"[:3]	'Pyt'	С начала
s[::step]	"Python"[::2]	'Pto'	С шагом
s[::-1]	"Python"[::-1]	'nohtyP'	Реверс

🔠 Методы изменения регистра
.lower() → все строчные
.upper() → все заглавные
.capitalize() → первая заглавная
.title() → каждое слово с заглавной
.swapcase() → инвертировать регистр

🔍 Методы поиска
.find(sub) → индекс или -1
.rfind(sub) → поиск с конца
.index(sub) → индекс или ошибка
.count(sub) → количество вхождений
.startswith(prefix) → начинается с?
.endswith(suffix) → заканчивается на?

🧹 Очистка и замена
.strip() → убрать пробелы с краёв
.replace(old, new) → замена
.split(sep) → разбить на список

✅ Проверочные методы (True/False)
.isalpha() → только буквы
.isdigit() → только цифры
.isalnum() → буквы+цифры
.islower() → все строчные
.isupper() → все заглавные
.isspace() → только пробелы

💫 Форматирование строк
# f-строки (рекомендуется)
name, age = "Иван", 25
f"{name} - {age} лет"  # 'Иван - 25 лет'

# Метод .format()
"{} - {} лет".format(name, age)

# %-форматирование (старое)
"%s - %d лет" % (name, age)

🔢 Коды символов (Unicode)
ord('A') → 65 (получить код)
chr(65) → 'A' (получить символ)
'A' < 'a' → True (65 < 97)
'а' > 'a' → True (1072 > 97)
Русские: А=1040, а=1072, Я=1071, я=1103
# Получить все коды от 'a' до 'z'
for i in range(ord('a'), ord('z') + 1):
    print(f"'{chr(i)}' = {i}")
# Или в виде списка
codes = [(chr(i), i) for i in range(ord('a'), ord('z') + 1)]
print(codes)

⚡ Быстрые советы
Строки неизменяемы — методы создают новые строки
Отрицательные индексы — работают с конца
Сравнение строк — лексикографическое (по кодам)
find() безопаснее index() — не вызывает ошибок
f-строки — современный стандарт форматирования
Для проверки регистра + цифр → s == s.lower() вместо s.islower()

🎓 Практические паттерны
# Реверс строки
reversed_s = s[::-1]

# Удалить все цифры из строки
cleaned = ''.join(char for char in s if not char.isdigit())

# Проверить, что строка содержит только русские буквы
all_russian = all('А' <= c <= 'я' or c in 'Ёё' for c in s)

# Шифр Цезаря (декодирование)
decoded = ''.join(chr((ord(c) - ord('a') - n) % 26 + ord('a')) for c in s)

*Создано в процессе изучения Module 1: Strings (9.1-9.8)*
*Автор: Ivan M gen-comf | Дата: 2025-12-17*
