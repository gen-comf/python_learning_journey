"""
📝 ЭКЗАМЕН по разделу "Строки" (Strings)
Курс: "Поколение Python"
Автор: gen-comf
Дата: 2025-12-17

Структура экзамена:
- [число] задач на различные темы раздела
- Тестирование понимания: индексация, срезы, методы строк
- Практическое применение: обработка текста, валидация

Задачи решаются последовательно, каждая задача комментируется.
"""

print("=== Задача 1: Каждый третий 3️⃣")
text = input()
result = ''

for i in range(len(text)):
    if i % 3 != 0:
        result += text[i]

print(result)


print("=== Задача 2: Замени меня полностью 🔄")
text = input()
print(text.replace('1', 'one'))


print("=== Задача 3: Удали меня полностью ❌")
text = input()
print(text.replace('@', ''))

print("=== Задача 4: Второе вхождение 2️⃣")
text= input()

if text.count('f') == 1:
    print('-1')
elif text.count('f') == 0:
    print('-2')
else:
    # 1. Находим индекс ПЕРВОГО вхождения
    first_index = text.find('f')

    # 2. Ищем 'f' НАЧИНАЯ С ПОЗИЦИИ ПОСЛЕ первого вхождения
    second_index = text.find('f', first_index + 1)
    
    print(second_index)


print("=== Задача 5: Переворот")
text = input()

# 1. Находим индексы
first_index = text.find('h')
last_index = text.rfind('h')  # Последнее вхождение, а не второе!

# 2. Разбиваем строку на 3 части
text_1 = text[:first_index + 1]          # До первого h (включая его)
text_2 = text[first_index + 1:last_index] # Между h и h (исключая оба)
text_3 = text[last_index:]               # От последнего h до конца (включая его)

# 3. Переворачиваем среднюю часть
reversed_text_2 = text_2[::-1]

# 4. Собираем результат
result = text_1 + reversed_text_2 + text_3

print(result)
