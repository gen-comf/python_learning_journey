# 🐙 Git Commands Cheatsheet
*Шпаргалка из курса Git/GitHub + личные заметки*

---

## 📋 **Быстрая навигация**
- [⚙️ Настройка и конфигурация](#️-настройка-и-конфигурация)
- [📦 Создание и клонирование](#-создание-и-клонирование)
- [🔄 Основной workflow](#-основной-workflow)
- [🌿 Ветки (Branches)](#-ветки-branches)
- [☁️ Удаленные репозитории](#️-удаленные-репозитории)
- [⏮️ Отмена изменений](#️-отмена-изменений)
- [💾 Временное сохранение](#-временное-сохранение)
- [🔍 Инспекция и сравнение](#-инспекция-и-сравнение)
- [💡 Советы и лучшие практики](#-советы-и-лучшие-практики)
- [🚀 Мои частые команды](#-мои-частые-команды)

---

## ⚙️ **Настройка и конфигурация**

| Команда | Описание | Пример |
|---------|----------|--------|
| `git config --global user.name "Имя"` | Установить имя пользователя | `git config --global user.name "Ivan"` |
| `git config --global user.email "email"` | Установить email | `git config --global user.email "ivan@example.com"` |
| `git config --list` | Показать все настройки | `git config --list` |
| `git config --global core.editor "editor"` | Установить редактор по умолчанию | `git config --global core.editor "code --wait"` |

## 📦 **Создание и клонирование**

| Команда | Описание | Пример |
|---------|----------|--------|
| `git init` | Создать новый локальный репозиторий | `git init` |
| `git clone <url>` | Склонировать удаленный репозиторий | `git clone https://github.com/user/repo.git` |
| `git remote add origin <url>` | Добавить удаленный репозиторий | `git remote add origin https://github.com/user/repo.git` |
| `git remote -v` | Показать список удаленных репозиториев | `git remote -v` |

## 🔄 **Основной workflow**

### **Работа с изменениями**
| Команда | Описание | Когда использовать |
|---------|----------|-------------------|
| `git status` | Показать состояние рабочей директории | Перед любыми действиями |
| `git add <file>` | Добавить файл в индекс | После изменений в файле |
| `git add .` | Добавить все изменения в индекс | Когда готов коммитить все изменения |
| `git commit -m "Сообщение"` | Создать коммит | Для фиксации изменений |
| `git commit --amend` | Исправить последний коммит | Если забыл добавить файл или исправить сообщение |

### **Просмотр истории**
| Команда | Описание | Полезные опции |
|---------|----------|----------------|
| `git log` | Показать историю коммитов | `--oneline`, `--graph`, `--all` |
| `git log --oneline` | Краткая история коммитов | Быстрый просмотр |
| `git log --graph` | История с визуализацией веток | Для сложных ветвлений |
| `git show <commit>` | Показать изменения в коммите | `git show abc123` |

## 🌿 **Ветки (Branches)**

### **Создание и переключение**
| Команда | Описание | Аналоги |
|---------|----------|---------|
| `git branch` | Показать список веток | `git branch -a` (все ветки) |
| `git branch <name>` | Создать новую ветку | `git branch feature/auth` |
| `git checkout <branch>` | Переключиться на ветку | Устаревший, но работает |
| `git checkout -b <branch>` | Создать и переключиться на ветку | Классический способ |
| `git switch <branch>` | Переключиться на ветку | Новый рекомендуемый способ |
| `git switch -c <branch>` | Создать и переключиться | Современная альтернатива |

### **Слияние и удаление**
| Команда | Описание | Предупреждение |
|---------|----------|----------------|
| `git merge <branch>` | Слить ветку в текущую | Предварительно обнови ветку |
| `git merge --abort` | Отменить слияние | При конфликтах |
| `git branch -d <branch>` | Удалить ветку | Только если слита |
| `git branch -D <branch>` | Принудительно удалить ветку | Осторожно! |

## ☁️ **Удаленные репозитории**

| Команда | Описание | Разница между fetch и pull |
|---------|----------|----------------------------|
| `git fetch` | Загрузить изменения с удаленного репозитория | Только загрузка, без слияния |
| `git pull` | Загрузить и слить изменения | `fetch` + `merge` |
| `git push` | Отправить изменения на удаленный репозиторий | `git push origin main` |
| `git push -u origin <branch>` | Отправить и установить отслеживание | Для первой отправки ветки |
| `git push --force` | Принудительная отправка | **Очень осторожно!** |

## ⏮️ **Отмена изменений**

| Команда | Описание | Когда использовать |
|---------|----------|-------------------|
| `git restore <file>` | Отменить изменения в файле | Если испортил файл |
| `git restore --staged <file>` | Убрать файл из индекса | Добавил не тот файл |
| `git reset --soft <commit>` | Отменить коммиты, сохранив изменения | Переделать коммит |
| `git reset --hard <commit>` | Полностью отменить коммиты | **Опасная команда!** |
| `git revert <commit>` | Создать коммит, отменяющий изменения | Безопасная отмена |

## 💾 **Временное сохранение**

| Команда | Описание | Аналоги |
|---------|----------|---------|
| `git stash` | Временно сохранить изменения | Быстро переключить контекст |
| `git stash list` | Показать список сохраненных изменений | Просмотр stash'ей |
| `git stash pop` | Восстановить последние изменения | Восстановить и удалить |
| `git stash apply` | Применить изменения без удаления | Восстановить, оставить в stash |
| `git stash drop` | Удалить сохраненные изменения | Очистка ненужного |

## 🔍 **Инспекция и сравнение**

| Команда | Описание | Полезные варианты |
|---------|----------|-------------------|
| `git diff` | Показать незакоммиченные изменения | `git diff --staged` |
| `git diff --staged` | Показать изменения в индексе | Перед коммитом |
| `git diff <commit1> <commit2>` | Сравнить два коммита | `git diff abc123 def456` |
| `git blame <file>` | Показать автора каждой строки файла | Для анализа кода |
| `git grep "текст"` | Поиск текста в файлах репозитория | Поиск по коду |

---

## 💡 **Советы и лучшие практики**

### ✅ **Всегда:**
1. **Проверяй `git status`** перед коммитом
2. **Используй `git diff`** для просмотра изменений
3. **Делай частые и атомарные коммиты** (одна задача = один коммит)
4. **Пиши осмысленные сообщения коммитов** в формате:




**Последнее обновление:** 2026-02-04 
**Автор:** Ivan (gen-comf)  
**Курс:** Git/GitHub - завершен ✅ 100%